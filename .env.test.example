# Used by the Vitest suite (npm test). Point this at a separate database from
# dev/prod — the tests wipe all rows between cases.
DATABASE_URL="sqlserver://localhost:1433;database=qrmenu_test;integratedSecurity=true;trustServerCertificate=true;encrypt=true"
